apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glustersubvol.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "glustersubvol.name" . }}
    helm.sh/chart: {{ include "glustersubvol.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "glustersubvol.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
